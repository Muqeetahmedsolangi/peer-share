{
  "buildCommand": "cd client && NEXT_TELEMETRY_DISABLED=1 npm run build",
  "outputDirectory": "client/.next",
  "installCommand": "cd client && npm install",
  "framework": "nextjs",
  "env": {
    "NEXT_TELEMETRY_DISABLED": "1"
  }
}
